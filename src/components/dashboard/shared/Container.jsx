const Container = ({children}) => {
    return (
        <div className='mx-w-[2520px] mx-auto xl:px-20 md:px-8 sm:px-2 px-4'>
            {children}
        </div>
    );
};

export default Container;
